[project]
name = "pf2e-codex-training"
version = "0.1.0"
requires-python = ">=3.13"
dependencies = [
    "torch>=2.9.0",
    "transformers>=4.40",
    "accelerate>=0.30",
    "datasets>=2.18",
    "huggingface-hub>=0.23",
    "peft>=0.10",
    "safetensors>=0.4",
]

[tool.uv]
extra-index-url = ["https://download.pytorch.org/whl/rocm7.2"]
